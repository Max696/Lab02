@model IEnumerable<WebApplication2.Models.Pais>

@{
    ViewBag.Title = "Post";
}

<h2>Post</h2>

<p>
    @Html.ActionLink("Regresar", "Index")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Grupo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th></th>
    </tr>
    @foreach (var i in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(show => i.Grupo)
            </td>
            <td>
                @Html.DisplayFor(show => i.nombre)
            </td>
            
        </tr>
    }
</table>
