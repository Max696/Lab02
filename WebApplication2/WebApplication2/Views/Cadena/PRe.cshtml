@model IEnumerable<WebApplication2.Models.Cadena>

    @{
        ViewBag.Title = "PRe";
    }

    <h2>Post</h2>
    <p>
        @Html.Action("Regresar", "Index");
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cadena);
            </th>
        </tr>
        @foreach (var i in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(show => i.cadena);
                </td>
            </tr>
        }
    </table>
